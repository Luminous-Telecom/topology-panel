# Agentes — Dude Topology Panel

Regras Cursor centralizadas em [`.cursor/rules/`](../.cursor/rules/) (workspace `dude/`).

| Regra | Escopo |
|-------|--------|
| `dude-topology-overview.mdc` | Sempre — repo, deploy, o que não publicar |
| `dude-topology-plugin.mdc` | `src/**` — links, grade, redes, código |
| `dude-topology-maps.mdc` | `examples/`, `dashboards/`, `scripts/` |

Antes de mudar links, grade ou travas de rede, leia `dude-topology-plugin.mdc`.
