---
description: Visão geral do projeto Dude Topology (Luminous) — escopo, repo e deploy
alwaysApply: true
---

# Dude Topology — visão geral

## Projeto

Plugin Grafana **`luminous-dude-topology-panel`** (`src/plugin.json`) para mapas estilo The Dude com status Zabbix.

- Repo: https://github.com/Luminous-Telecom/topology-panel
- Id do plugin: `luminous-dude-topology-panel`

## O que NÃO vai pro GitHub

Manter **somente local** (`.gitignore`):

- `examples/` — JSON dos mapas
- `dashboards/` — dashboards Grafana
- `scripts/` — build/upload/deploy internos
- `dist/`, `node_modules/`, `*.tar.gz`

Não commitar credenciais, `.env` ou senhas em scripts.

## Versão e release

1. Bump **`src/plugin.json`** e **`package.json`** (mesma versão)
2. `npm run build`
3. Deploy: `dist/` → `/var/lib/grafana/plugins/luminous-dude-topology-panel/`
4. Reiniciar `grafana-server`
5. Commit/push só código do plugin

## UI e idioma

- Labels, menus e hints em **português**
- Diffs **mínimos** — não refatorar fora do escopo

## Grafana

- `allow_loading_unsigned_plugins = luminous-dude-topology-panel`
- Upload dashboard preferir LAN; URL pública pode retornar 403 (Cloudflare)
