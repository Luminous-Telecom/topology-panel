---
description: Inventário de helpers e hooks já existentes — reutilize antes de escrever lógica nova
globs: src/**/*
alwaysApply: false
---

# Anti-duplicação — reutilize antes de criar

## Objetivo

Evitar reimplementar lógica que já existe e está testada.

## Escopo

Qualquer arquivo em `src/`. Consulte a tabela **antes** de escrever helper novo.

## Regras obrigatórias

- Precisa de um caso novo numa área da tabela? **Estenda a função existente** (parâmetro opcional,
  novo branch) em vez de criar equivalente em outro arquivo.
- Notou 2+ blocos quase idênticos enquanto edita? Extraia na mesma tarefa: lógica pura para
  `utils/`, lógica com estado para `hooks/`.
- Não duplique o `QueryIndex`: status e hosts vêm de `buildZabbixDirectIndex`.

## Dados de status

| Necessidade | Já existe em |
|---|---|
| Formato do índice de status (metadata, hosts, refIds, buckets) | `QueryIndex` em `services/queryIndex.ts` |
| Montar o índice a partir do Zabbix | `buildZabbixDirectIndex` em `services/zabbixDirectIndex.ts` |
| Buscar/repetir a busca do último valor no Zabbix | `hooks/useZabbixDirectIndex.ts` (polling) + `fetchZabbixStatusViaQuery` em `utils/zabbixDatasourceQuery.ts` (`ds.query()` Metrics: `zabbixGroupsFilter` + item) |
| Inventário de itens de interface | `fetchZabbixHostInterfaceItemsViaQuery` em `utils/zabbixDatasourceQuery.ts` (`ds.query()` Metrics: um host, item `/.*/`; filtro da key no parse) |
| Gate de intervalo/watchdog do polling | `canStartPolledFetch` em `utils/pollingGate.ts` |
| Lastvalue de itemids de cabo | `fetchZabbixItemLastValues` em `utils/zabbixApi.ts` (`item.get` em paralelo ao status) |
| Chaves de cabo sem itemid numérico | `collectLinkMetricKeys` + `aliasLastValuesByItemKey` em `utils/linkMetricsRuntime.ts`; resolve via `resolveZabbixItemIdsByKeys` em `utils/zabbixApi.ts` |
| Problemas Warning+ | `fetchZabbixHostProblemsViaQuery` em `utils/zabbixDatasourceQuery.ts` |
| Listar grupos de host do Zabbix | `hooks/useZabbixHostGroups.ts` (`metricFindQuery`) |
| Listar nomes de item do Zabbix | `hooks/useZabbixItemNames.ts` (`metricFindQuery` Item) |
| Hosts e valores por `refId`, metadata, lista de refIds | `QueryIndex` (mesmo arquivo) |
| Display de status por host e refId | `hostDisplayByRefIdFromIndex` em `services/queryIndex.ts` |
| Status/cor/rótulo a partir de um valor numérico | `utils/statusMapping.ts` (`resolveHostStatusDisplay`, `resolveHostStatusFromValue`, `resolveStatusColor`, `resolveMappingLabel`) |
| Série temporal do gráfico de hover | `parseHoverSeriesFromFrames` + `lookupHostHoverSeries` (poll de status, sem query no hover) |
| Relógio do dashboard no hover (`skipDataQuery` ignora `data.timeRange`) | `panelDataWithDashboardTimeRange` em `utils/hostTimeSeries.ts` |
| Estatísticas agregadas de rede/submapa | `utils/networkStats.ts` |
| Casar hosts do mapa com hosts do índice | `mergeMapWithQueryHosts` / `resolveHostDescription` / `resolveHostVisibleName` em `utils/mapSync.ts` |
| Resolver IP/chave de um host do mapa | `utils/hostLookup.ts` (`collectHostLookupCandidates`, `collectHostMetadataForMaps` para problemas) |
| Inventário de interface por extremo do cabo | `hooks/useLinkPeerInterfaces.ts` (`combinePeerInterfaceLoadError`) |
| RX/TX do cabo na orientação do mapa (origem vs destino) | `resolveLinkMapTrafficMetrics` em `utils/linkMetricsRuntime.ts` |
| Limiares/cores de degradação do cabo | `utilizationThresholdsFromOptions`, `linkDegradationColor`, `resolveLinkUtilizationLevel` |
| Hosts e refIds do índice | `utils/queryHosts.ts` |
| Grupos Zabbix da árvore (query de status, estável ao navegar) | `collectAllSubmapGroups` em `utils/queryHosts.ts` |
| Grupos gravados no nó submapa (`queryRefIds` ou `queryRefId` legado) | `submapQueryRefIds` em `utils/queryHosts.ts` |
| Grupos efetivos do atalho (próprios + dono do mapa interno) | `effectiveSubmapQueryRefIds` em `utils/queryHosts.ts` |
| Grupos no casing do Zabbix a partir da metadata | `zabbixGroupsFromHostMetadata` em `utils/queryHosts.ts` |
| Lista de hosts dos pickers (Adicionar host, editar nó) | `utils/queryHostPicker.ts` |
| Guards `isHostNode`/`isSubmapNode`/`findNodeById` | `utils/topologyNodes.ts` |
| Medida de texto e caixa do nó | `utils/nodeLayout.ts` |

## Mapa, geometria e interação

| Necessidade | Já existe em |
|---|---|
| Add/move/update/remove de nó, travas do mapa | `utils/mapEdits.ts` |
| Criar, editar e remover cabo (inclui `linkKey`/`linksMatchEndpoints`/`upsertLinkWithInterfaces`) | `utils/mapLinkEdits.ts` |
| Espelhar cabo entre submapas (raiz e mapa de destino) | `syncInterSubmapCounterpartLinks` em `utils/interSubmapLinks.ts` |
| Excluir cabo entre submapas em cascata (espelhos e hops órfãos) | `removeInterSubmapCounterpartLinks` / `removeMissingInterSubmapCounterparts` em `utils/interSubmapLinks.ts` |
| Aplicar ícone, credenciais ou tamanho a vários nós | `utils/mapBulkEdits.ts` |
| Traduzir o formulário de propriedades em patch do nó | `buildNodeEditPayload` em `utils/nodeEditPayload.ts` |
| Validar estrutura do mapa | `validateTopologyMap` em `utils/mapValidation.ts` |
| Busca do mapa | filtro em `components/canvas/TopologyMapSearch.tsx` |
| Converter coordenada de tela para coordenada do mapa | `utils/mapCoords.ts` |
| Posições do grupo arrastado, limites e vizinhos das guias | `utils/dragMove.ts` |
| Matemática de zoom (roda e pinch), limites de escala | `utils/zoomMath.ts` |
| Limites/bounding box do mapa | `utils/mapBounds.ts` |
| Guias de alinhamento durante o arraste | `computeAlignGuides` em `utils/alignGuides.ts` |
| Pan automático ao arrastar na borda | `utils/edgePan.ts` |
| Geometria do link: path, waypoint mais próximo, âncora do rótulo | `computeLinkGeometry`, `buildLinkPathD`, `closestPointOnPolyline`, `nearestWaypointIndex`, `linkLabelAnchor` em `utils/linkGeometry.ts` (ângulo e raio de canto são internos — não reimplemente, use estas funções) |
| Espessura/rótulo de link por banda (Mbps/Gbps) | `utils/linkBandwidth.ts` |
| Formato de tráfego (bps, rótulo do cabo, intervalo) | `formatBitsPerSecond`, `formatLinkMapTrafficLabel`, `formatPluginRefreshInterval` em `utils/zabbixAdapter/formatTraffic.ts` |
| Inventário de interfaces do host (agrupar / escolher por alias) | `parseZabbixInterfaceItems`, `groupInterfacesByHost`, `pickHostInterfaces` em `utils/zabbixAdapter/parseInterfaceItems.ts` |
| Animação de fluxo no link | `utils/linkFlow.ts` + `hooks/useLinkFlowAnimation.ts` |
| Compartilhamento estrutural de identidade entre refreshes | `structuralShare` / `structuralShareMap` em `utils/structuralIdentity.ts` |
| Identidade estável de valor JSON-like | `useStableIdentity` em `hooks/useStableIdentity.ts` |
| Identidade fixa de handler | `useStableCallback` em `hooks/useStableCallback.ts` |
| Recorte de nós/cabos pela viewport | `visibleWorldRect` em `utils/viewportCulling.ts` |
| Wheel/toque sobre overlay do mapa (NOC, busca, alertas) | `eventTargetsElement`, `eventTargetsMapOverlay`, `wheelTargetsScrollableDescendant` em `utils/domScroll.ts` |
| Copiar/colar nós e links | `utils/topologyClipboard.ts` + `hooks/useTopologyClipboardActions.ts` |
| Undo/redo | `hooks/useMapHistory.ts` |
| Toque no host (peek ICMP / Tools) | `resolveHostTouchTap` em `utils/nodeTap.ts` |
| Seleção, arraste e viewport | `hooks/useTopologySelection.ts` (`nextSelectedNodeIds` / `nextSelectedNodeIdsOnPointerDown`), `useTopologyDragController.ts`, `useTopologyViewport.ts` |
| Medida do painel, tela cheia e gestos de zoom/toque | `hooks/useViewportSize.ts`, `useFullscreen.ts`, `useCanvasZoomGestures.ts` |
| Grade do mapa | `hooks/useGridLines.ts` |
| Layout dos nós e estatística das regiões | `hooks/useNodeLayouts.ts` |
| Cabos filtrados e ordenados para render | `hooks/useRenderLinks.ts` |
| Cores do minimapa (mesmas do mapa grande) | `hooks/useMinimapColors.ts` |
| Hosts agregados do submapa e hosts já desenhados no mapa pai | `submapHostListForNode`, `parentMapHostKeys`, `innerHostsForSubmapNode`, `shouldOpenLinkInterfaceModal` em `utils/submapHosts.ts` |
| Caixa da região no outro submapa (não a caixa batizada com host) | `pickCounterpartSubmapBox` em `utils/submapHosts.ts` |
| Itens de menu de contexto (copiar, colar, lote, excluir, cabo) | `utils/contextMenuItems.ts` |
| Itens da legenda a partir das opções do painel | `buildLegendItems` em `utils/legendItems.ts` |

## Grafana, aparência e UI

| Necessidade | Já existe em |
|---|---|
| Buscar dashboards (`dash-db`) com estado de loading | `hooks/useGrafanaDashboards.ts` |
| Opções de `Select`/`MultiSelect` de dashboard, filtro por tag | `utils/grafanaDashboards.ts` |
| Detectar modo de edição do dashboard | `hooks/useDashboardEditMode.ts` |
| Detectar lista de reprodução / kiosk | `hooks/useGrafanaPlaylistPlayback.ts` + `utils/grafanaPlaylist.ts` |
| Navegar por variável de dashboard | `hooks/useDashboardVariableNav.ts` |
| Resolver cor de opção do painel (hex/rgb/nome de tema) | `resolvePanelColor` em `utils/panelColors.ts` |
| Contraste de texto sobre fundo colorido | `utils/colorContrast.ts` |
| Ícone de host e de rede | `utils/hostIcons.tsx`, `utils/networkIcons.tsx`, registro em `utils/customIcons.ts` |
| Abrir Winbox/SSH/Telnet e validar IPv4 | `utils/hostTools.ts`, `utils/ipv4.ts` |
| Fechar overlay com Esc | `hooks/useEscapeKey.ts` |
| Rótulo de bloco quando o conteúdo não é um único form control | `components/FieldReadout.tsx` |
| Select de interface do cabo (Novo link / Editar link) | `components/LinkInterfaceSelectField.tsx` (`interfaceOptionValue`) |
| Chrome visual de modal e overlay do mapa | `components/overlayChrome.ts` + `components/TopologyModal.tsx` |
| Ficha só leitura do host (duplo clique fora do editor) | `HostInfoModal` em `components/HostInfoModal.tsx` |
| Portal de overlay em tela cheia | `overlayPortalRoot` em `utils/overlayPortal.ts` |

## Exceções

Nenhuma. Se a função existente realmente não serve, explique o porquê na resposta antes de criar
outra.
